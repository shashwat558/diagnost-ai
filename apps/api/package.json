{
  "name": "@diagnost/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "node dist/index.js"
  },
  "dependencies": {
    "@clickhouse/client": "^1.8.0",
    "@diagnost/core": "workspace:*",
    "@diagnost/db": "workspace:*",
    "@diagnost/queue": "workspace:*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
